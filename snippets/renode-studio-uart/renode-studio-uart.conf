# Renode's nRF52840 model cannot initialize USB. Route console and Studio RPC
# over real UART peripherals, and use this module's test-only transport.
CONFIG_ZMK_USB=n
CONFIG_ZMK_BLE=n
CONFIG_BOARD_SERIAL_BACKEND_CDC_ACM=n
CONFIG_LOG=y
CONFIG_LOG_PROCESS_THREAD_STARTUP_DELAY_MS=0
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_UART_INTERRUPT_DRIVEN=y
CONFIG_ZMK_STUDIO_TRANSPORT_UART=n
CONFIG_ZMK_RENODE_STUDIO_UART_TRANSPORT=y
